apiVersion: v1
kind: Service
metadata:
  name: dorfarchiv
  namespace: dorfarchiv
  labels:
    app: dorfarchiv
spec:
  selector:
    app: dorfarchiv
  ports:
    - name: http
      port: 8080
      targetPort: 8080
